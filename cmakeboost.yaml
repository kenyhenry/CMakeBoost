# project.yaml
project:
  name: MyProject
  version: 1.0.0
  entrypoint: src/main.cpp

# dependencies:
  # - name: opengl
  #   version: 3.3.0
  # - name: glfw
  #   version: 3.3.2

build:
  compiler: g++
  flags: -O2 -Wal

run:
  args: ["arg1", "arg2"]
