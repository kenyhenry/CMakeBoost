# project.yaml
project:
  name: MyProject
  version: 1.0.0
  entrypoint: src/main.cpp

dependencies:
  - name: fmt
    version: 11.0.1
    repo: https://github.com/fmtlib/fmt.git

build:
  compiler: g++
  flags: -O2 -Wal

run:
  args: ["arg1", "arg2"]
